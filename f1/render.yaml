services:
  - type: web
    name: f1-poke-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: fastmcp run src/server.py:mcp --transport streamable-http --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 8000
